# 项目说明

这是一个用于本地验证 YOLO 基础陈列识别的示例项目。

## 相关资料

- 架构设计：`设计.md`
- YOLO/Ultralytics 官方文档：https://docs.ultralytics.com

## 开发约定

- 本项目以本地测试与验证为主。
- 修改实现前，先阅读 `设计.md` 以遵循既定架构。
- 与 YOLO 使用和配置相关的问题，优先参考 Ultralytics 官方文档。
- 每次执行用户提出的代码、配置、脚本、数据流程、训练流程、推理流程、文档结构等修改动作之前，必须先进行计划：先阅读相关上下文，按下方命名规则在 `docs/plans/` 创建或更新本次计划 Markdown，然后把计划内容展示给用户，等待用户明确确认后再执行。用户只问问题、只要求解释、只要求查看状态时不需要创建计划。
- 后续阶段性结论、实验结果、数据导入结果、训练/推理结果，也需要同步沉淀到 `docs/plans/` 下对应 Markdown 文件；若已有今日相关计划/记录则追加或更新。
- `docs/plans/` 文件命名规则采用固定格式：`YYYY-MM-DD_N_plan_TOPIC.md`。
  - `YYYY-MM-DD`：当前日期，例如 `2026-08-25`。
  - `N`：当天递增序号，从 `1` 开始；同一天每次新的重要计划或沟通主题递增，不复用已存在序号。
  - `plan`：固定字面量，表示该文件是计划/阶段记录。
  - `TOPIC`：本次沟通计划主题，使用简短中文或中英混合描述，不使用空格，必要时用连字符或下划线。
  - 示例：`2026-08-25_1_plan_EC2工作流初步创建.md`、`2026-08-25_2_plan_EC2工作流按照新要求优化.md`、`2026-08-25_3_plan_EC2上传脚本.md`、`2026-08-25_4_plan_make命令拆分.md`、`2026-08-25_5_plan_make参数说明.md`。
  - 若只是修正同一计划的小问题，可更新对应文件内容；若形成新的执行范围或新的重要决策，应创建新的递增序号文件。
- 后续如果识别方案、数据流程、训练流程、OCR/YOLO/多模态架构发生优化或调整，必须同步更新 `设计.md`，确保架构文档与当前实现一致。
- 所有代码必须写上详细的注释，并使用 `pylint` 进行静态检查。

## 思考和任务展示

- 使用中文进行回复和任务展示